# DotStroke — ドット絵ツールの仕様と知見

## 1. ツールの目的

DotStroke は、Playdate 向けの小さな 1 bit グラフィックを、ピクセル単位のベクター図形として作成する Rust + egui 製エディタである。編集データは `pdvector` JSON として保存し、完成結果を PNG または Playdate SDK 用 Lua コードとして出力できる。

このツールの中心的な考え方は「ドット絵を直接ビットマップ編集する」のではなく、「ピクセル格子にスナップした図形を保持し、必要なときに 1 bit 表現へ変換する」ことにある。これにより、再編集、図形の順序変更、スタイル変更、Lua への変換を両立している。

## 2. 技術構成

- 言語: Rust 2021 Edition
- GUI: `eframe` / `egui` 0.35
- システムメニュー: `muda` / `winit`
- JSON: `serde` / `serde_json`
- 画像入出力: `image`
- ファイルダイアログ: `rfd`
- クリップボード: `arboard`

起動方法:

```sh
cargo run
```

実装は機能別に分離されている。主な責務は次のとおり。

| ファイル | 責務 |
| --- | --- |
| `src/model.rs` | JSON に保存するドキュメント、レイヤー、図形、スタイル |
| `src/app/state.rs` | GUI 状態、選択、表示変換、ダイアログなどのアプリ状態 |
| `src/app/interaction.rs` | 選択、スナップ、移動、複製、削除、並べ替え、グループ化 |
| `src/app/canvas.rs` | キャンバス表示とポインター操作 |
| `src/app/vector_render.rs` | egui 上のベクタープレビュー |
| `src/app/raster.rs` | 1 pixel 単位のラスタライズと PNG 用ピクセル生成 |
| `src/app/lua_export.rs` | Playdate Lua コード生成 |
| `src/io.rs` | JSON/PNG の入出力と最近使ったファイルの保存 |
| `src/editor.rs` | Undo/Redo 履歴と幾何計算の基礎処理 |
| `src/config.rs` | 操作判定、レイアウト、色などの調整可能な定数 |

## 3. 機能仕様

### 3.1 キャンバス

- 新規ドキュメントの初期値は 32 × 32 px。
- プリセットは 16 × 16、32 × 32、400 × 240 px。
- 解像度変更は 8〜400 px の範囲で、8 px 刻みに丸める。
- 座標系は左上原点。既定のスナップは整数座標。
- 丸め方法は nearest / floor / ceil から選択する。
- ホイールでズーム、中ボタンドラッグまたは Space + 左ドラッグでパン。
- ズーム範囲の下限は 0.25 倍。上限はビューポートの短辺を 8 で割った値を基準に決める。
- Pixel preview で、ベクター表示と実際のラスタライズ結果を切り替えられる。
- 透明背景はチェッカーボードで表示する。ズームに応じてグリッドの間引きも変える。

### 3.2 描画ツール

利用できるツールは次のとおり。塗りつぶし版は JSON 上では同じ図形種別に正規化され、`style.fill` で区別される。

- 選択
- 1 pixel
- 線分
- ポリライン
- ポリゴン / 塗りつぶしポリゴン
- 矩形 / 塗りつぶし矩形
- 角丸矩形 / 塗りつぶし角丸矩形
- 楕円 / 塗りつぶし楕円
- パス

複数点の図形は右クリックまたは Enter で確定する。ポリゴンは 3 点以上、その他の複数点図形は 2 点以上が確定条件となる。

### 3.3 スタイル

- 色: black / white / clear
- 合成: normal / XOR
- 線幅: 1〜8 px
- 線端: butt / round / square
- 角丸半径: 0〜16 px
- 塗りつぶし: 図形ツールの選択によって設定
- ディザ: none、diagonal line、vertical line、horizontal line、screen、Bayer 2×2 / 4×4 / 8×8、Floyd–Steinberg、Burkes、Atkinson

選択ツールで 1 つの図形を選択中は、スタイル UI がその図形の値を表示し、変更を直接反映する。描画ツールの使用中は、これらが次に作る図形の初期値となる。

ディザ選択アイコンは `assets/dither_icons/` の PNG を起動時に読み込む。そのため、コードを変更せずにアイコンを差し替えられる。

### 3.4 選択とベクター管理

- キャンバス上の図形と制御点を選択・移動できる。
- 図形または制御点の移動後は、選択した丸め方法でグリッドにスナップする。
- ベクター一覧で名前変更、表示/非表示、削除、複製、上下移動、ドラッグによる順序変更ができる。
- Cmd/Ctrl を使った追加選択と Shift を使った範囲選択に対応する。
- 複数の図形は Cmd/Ctrl+G でフォルダ化できる。選択中のフォルダに同じ操作をすると解除される。
- フォルダの入れ子は禁止されている。
- フォルダは JSON 上で `kind: "group"` の図形として表現し、中身を `children` に保持する。描画と Lua 出力では再帰的に子要素を処理する。

### 3.5 ファイル操作と履歴

- JSON の新規作成、読み込み、上書き保存、別名保存。
- 最近開いたファイルを最大 10 件保存し、起動時に最後のファイルを復元する。存在しないパスは履歴読み込み時に除外する。
- Undo/Redo はドキュメント全体のスナップショットを保存する方式。
- 保存済みドキュメントと現在のドキュメントを JSON 化して比較し、未保存状態をウィンドウタイトルの `*` と終了確認に反映する。
- リファレンス画像は別ウィンドで読み込み、切り替え、ズーム、パンできる。これは作業用の参照であり、ドキュメント JSON には保存されない。

主なショートカット:

| 操作 | macOS | その他の環境 |
| --- | --- | --- |
| 新規 | Cmd+N | Ctrl+N |
| 開く | Cmd+O | Ctrl+O |
| 保存 | Cmd+S | Ctrl+S |
| 別名保存 | Cmd+Shift+S | Ctrl+Shift+S |
| PNG 出力 | Cmd+Shift+E | Ctrl+Shift+E |
| Lua コピー | Cmd+P | Ctrl+P |
| Undo / Redo | Cmd+Z / Cmd+Y | Ctrl+Z / Ctrl+Y |
| グループ化・解除 | Cmd+G | Ctrl+G |

### 3.6 出力

#### pdvector JSON

ドキュメントの核となる構造は次のとおり。`serde(default)` により、新しいフィールドがない旧データも既定値で読み込める。

```text
Document
├─ format: "pdvector"
├─ version: 1
├─ offset: bool
├─ target: SDK、幅、高さ、座標系、スナップ、丸め、クリップ
├─ canvas: 背景、ディザ基準
├─ optimize: 共線線分の結合、重複点除去、簡略化許容値
└─ layers[]
   └─ objects[]
      ├─ type / name / points / closed / visible
      ├─ style: color、blend、width、cap、fill、radius、dither_pattern
      ├─ transform: 将来拡張用の JSON 値
      └─ children[]: グループの子要素
```

モデルは複数レイヤーを保持できるが、現状の UI は主に現在レイヤー内のオブジェクト編集に焦点を当てている。

#### PNG

ドキュメントのターゲット寸法と同じ大きさの RGBA PNG を出力する。背景は透明とし、表示中のレイヤーと図形のみをラスタライズする。

#### Playdate Lua

通常出力は Playdate の `playdate.graphics` API を直接呼び出す。対応例は `drawPixel`、`draw/fillRect`、`draw/fillRoundRect`、`draw/fillEllipseInRect`、`draw/fillPolygon`、`drawLine` など。スタイルは Playdate の色、XOR、線幅、線端、ディザ定数へ変換する。

- Offset 出力では `ox` / `oy` を座標に加算し、描画位置を呼び出し側から移動しやすくする。
- Animation Lua は `drawPrimitive(offset_x, offset_y, rotation, scale_x, scale_y, outline_width)` として出力し、平行移動、回転、X/Y 拡大率、輪郭幅を引数化する。回転中心はキャンバス中心。
- ディザ状態は Playdate で後続描画に残るため、パターンのない図形へ移る際に `kDitherTypeNone` へ明示的に戻す。
- 最後の表示図形が white の場合は、呼び出し後の状態漏れを避けるため black へ戻す。

## 4. 実装で得られた知見

### 4.1 ベクターを正とし、ピクセル表示を別経路で検証する

編集中のベクター表示と PNG 用ラスタライザは、同じ図形を別々に描画する。これはズーム時の見やすさと最終的な 1 pixel 結果の正確さを両立させる上で有効である。一方で、新しい図形を追加する際は、ベクター表示、ラスタ表示、Lua 出力の 3 経路すべてを同時に更新しなければならない。

そのため、「画面上で見える」だけでは完了とせず、Pixel preview、PNG、Lua の出力差分を検証する必要がある。

### 4.2 座標変換とスナップは中央化する

画面座標とドキュメント座標の変換は `ViewTransform` に集約し、スナップは nearest / floor / ceil を 1 か所で切り替える。ズーム中のヒットテスト許容幅は `8 / zoom` とし、画面上のクリックしやすさを一定に保っている。

この分離がないと、ズーム倍率によって選択位置やドラッグ量がずれる。描画、選択、制御点操作は必ずドキュメント座標に変換してから処理するのが安全である。

### 4.3 実行時状態を漏らさない

Playdate の描画 API の色、線幅、線端、ディザはステートフルである。とくにディザは設定したままにすると、次の図形まで意図せず影響を受ける。出力器は「必要な状態を設定する」だけでなく、「不要になった状態を明示的に解除する」必要がある。

### 4.4 画面表示用のツール名と保存形式を分ける

`fill_rect` や `fill_circle` は UI 上のツール名であり、保存時には `rect` / `ellipse` + `style.fill` に正規化される。これにより、レンダラーと出力器の分岐が増えすぎるのを防げる。ツールの数とデータモデルの種別数は一致させる必要がない。

### 4.5 スナップショット履歴は小規模ドキュメントに適する

Undo/Redo で個別コマンドの逆操作を実装せず、変更前の `Document` 全体を clone して保持する方式は、小さなドット絵データでは単純で壊れにくい。新機能でモデルにフィールドを追加しても、個別の Undo 実装を追加する必要が少ない。

一方、大きな画像や大量のベクターを内包するようになれば、メモリ使用量と clone コストが増える。参照画像を `Document` の外側に保持する現在の構成は、この点でも合理的である。

### 4.6 階層化では入れ子の仕様を早期に決める

グループを `VectorObject` の再帰構造にすると、描画と出力は自然に再帰処理できる。ただし、選択、ヒットテスト、並べ替え、スタイル変更の対象が「フォルダ」か「子要素」かを定義する必要がある。現状は入れ子を禁止することで UI と選択状態をシンプルに保っている。

### 4.7 複数選択では「主選択」と「選択集合」の同期が重要

実装は後方互換のための単一選択 `selected` と、複数選択 `selected_objects` の両方を保持する。並べ替えや削除でインデックが変わるたびに両方を更新する必要がある。この種の二重状態は便利だが、不整合の温床にもなる。将来的には、選択集合と最後に選択した要素から主選択を導出する形に統一する余地がある。

### 4.8 汎用 JSON 値は互換性に強いが、仕様が暗黙的になる

`canvas`、`optimize`、`transform` は型付き struct ではなく `serde_json::Value` で保持される。未知のフィールドを壊さず読み書きしやすい半面、編集時の型安全性と自己文書性が下がる。これらを UI から本格的に編集する段階で、型付きモデルへの移行を検討するとよい。

## 5. 現状の制約と注意点

- モデルは複数レイヤーに対応するが、レイヤーの追加・削除・切り替えを中心とした UI はまだ十分ではない。
- フォルダの入れ子は不可。子要素単位の編集 UI も限定的。
- `transform` データは保持されるが、通常のキャンバス編集は主に点座標を直接変更する。
- Undo/Redo はドキュメントのみを対象とし、ズーム、パン、参照画像などの UI 状態は対象外。
- ヒットテストは図形の幾何そのものよりも、主に制御点から得た境界ボックスを使う。重なりが多い図形では意図と異なる選択になる可能性がある。
- PNG と egui のベクター表示は別実装のため、線幅、楕円、角丸、ディザの端で差が出ないかを回帰確認する必要がある。
- Lua 生成は文字列組み立てである。出力モードが増えると重複しやすいため、出力例を固定したスナップショットテストが有効。

## 6. 今後の改善優先度

1. 各図形に対するラスタ結果と Lua 文字列の自動テストを追加する。
2. レイヤーの追加、削除、並べ替え、表示切り替えの UI を整備する。
3. 選択状態をインデックではなく安定 ID で管理し、並べ替えや削除時の同期処理を簡単にする。
4. 共通の図形表現または中間表現を導入し、ベクター表示、ラスタ化、Lua 生成の仕様差を減らす。
5. JSON の `format` / `version` を使った明示的なマイグレーション方針を定める。
6. グループの子要素編集や入れ子を支持する場合は、選択パスと階層的 transform の仕様を先に固める。

## 7. 機能追加時のチェックリスト

新しい図形、スタイル、出力機能を追加するときは、少なくとも次を確認する。

- JSON のデフォルト値と旧データの読み込み
- ツール UI と保存用図形種別の正規化
- 描画中プレビュー
- 通常ベクター表示
- Pixel preview と PNG ラスタライズ
- ヒットテストと制御点編集
- 移動、複製、削除、並べ替え、グループ内描画
- Undo/Redo の履歴保存タイミング
- 通常 Lua、Offset Lua、Animation Lua
- 非表示レイヤー/図形の除外
- 色、XOR、ディザなどの出力後状態のリセット
- macOS のネイティブメニューとクロスプラットフォームショートカット

---

本文書は、2026 年 9 月 10 日時点の `main` ブランチの実装を基に、現在の動作仕様と実装上の知見をまとめたものである。
